Rule 12.1 requires that expressions whose meaning depends on C’s precedence rules be parenthesised to make the intended grouping explicit. The `if` statement contains the compound condition `x > 0 && y < 100 && x != y`, which relies on the fact that comparison operators (`>`, `<`, `!=`) bind tighter than the logical AND (`&&`). Because the code omits parentheses around each comparison or around the entire logical chain, it depends on the reader’s knowledge of the precedence table rather than on explicit grouping, thereby violating the rule. The AST shows a chain of `&&` nodes whose operands are comparison nodes, confirming that the grouping is implicit and not parenthesised.