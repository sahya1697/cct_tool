CORRECTED:
```c
int check_limits(int x, int y) {
    if ((x > 0) && (y < 100) && (x != y)) {
        return 1;
    }
    return 0;
}
```

EXPLANATION:
Parentheses were added around each comparison to make the operator precedence explicit, satisfying MISRA‑C:2004 Rule 12.1 while preserving the original logic.