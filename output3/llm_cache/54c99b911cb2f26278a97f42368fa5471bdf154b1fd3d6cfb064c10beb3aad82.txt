The rule applies here because the code contains a function call with a side effect (log_error) as part of an expression on the RHS of the && operator, which has unspecified order of evaluation. The code violates the rule because the value of the expression is not guaranteed to be the same regardless of the order of evaluation, due to the potential for the log_error function to be called before or after the len > 0U condition is evaluated. This is evidenced by the LLM inference that the target line contains a function call with a side effect, which is part of the expression on the RHS of the && operator.