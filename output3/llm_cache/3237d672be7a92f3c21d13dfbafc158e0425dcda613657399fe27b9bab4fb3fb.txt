The rule applies here because it is a function call, and the function's declaration is not shown, but the function's behavior is inferred to be relevant to the rule. The code violates the rule because the function `recursive_sum` is called with a non-const argument, implying that the function may modify the object pointed to by its parameter. The evidence used is LLM inference, as the rule is triggered by the function call and the lack of explicit const qualification in the function's declaration.