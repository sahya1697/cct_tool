This rule applies here because the code contains a recursive function call, which poses a risk of stack overflow in safety-critical systems. The code violates this rule by directly calling the function `recursive_sum` within itself, as evident from the line marked with >>>. The evidence used was LLM inference, which detected the direct function call and matched it with the rule description.