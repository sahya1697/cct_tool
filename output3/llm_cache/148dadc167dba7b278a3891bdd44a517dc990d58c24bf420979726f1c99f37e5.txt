CORRECTED:
uint16_t raw;
for (uint16_t i = 0U; i < MAX_SENSORS; i++)
{
    raw = read_sensor_raw(i);
}

EXPLANATION:
The fix is to declare the loop counter variable 'i' as a local variable within the loop, rather than reusing an external variable 'i' which could be modified elsewhere.