CORRECTED:
>>> int32_t validate_packet(const uint8_t *pkt, uint16_t len)
    {
        uint32_t  addr;
        uint8_t   *ptr = pkt;
        ptr += len;
        // ... rest of the code remains the same ...
    }

EXPLANATION:
The fix replaces the explicit pointer arithmetic with array indexing by using a pointer variable to increment the address.