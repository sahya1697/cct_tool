The rule applies here because the code snippet is a function call, and the function being called is likely a function prototype that is being audited for compliance. The code violates the rule because the function prototype for `log_formatted` is not shown, but based on the function call, it is likely that the function prototype does not have a pointer parameter declared as `const`. This is inferred from the LLM (Language Model) analysis, which detected a potential violation of the rule based on the function call pattern.