The rule applies here because the code snippet is a function implementation, but the rule's amplification text emphasizes the importance of const qualification in function interfaces, implying that the principle should be applied broadly. The code violates the rule by not declaring the pointer parameter "n" as a pointer to const, despite not modifying the object it points to. The evidence used is LLM inference, as indicated by the "Detection source" field, which suggests that the rule's application was inferred by the LLM (Language Model) based on its understanding of the rule's intent and the code's structure.