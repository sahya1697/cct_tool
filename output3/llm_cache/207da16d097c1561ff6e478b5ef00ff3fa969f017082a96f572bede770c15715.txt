The rule applies here because the code uses explicit pointer arithmetic to access an array element, which is not allowed by Rule 17.4. The code violates this rule by using the expression `*(ptr + i)` instead of the array indexing syntax `ptr[i]`, which is a clear indication of explicit pointer arithmetic. The evidence used is the LLM inference, which detected the use of `*(ptr + i)` as a pointer arithmetic operation.