The rule applies here because the code contains a logical && operation with a side effect on the RHS, which requires the operands to be primary-expressions. The code violates this rule because the RHS operand of the && operation is a function call with a side effect (log_error), which is not a primary-expression. The evidence used was LLM inference, which identified the log_error call as a side effect and determined that the RHS operand is not a primary-expression.