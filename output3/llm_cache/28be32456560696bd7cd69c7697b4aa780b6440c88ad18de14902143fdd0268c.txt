The rule applies here because, although it specifically mentions function prototypes, its amplification emphasizes protecting the object pointed to, which is relevant to the code's function body. The code violates the rule because it modifies the object pointed to by `ptr` through the expression `ptr = ptr + (++i)`, which implies that the object is being modified. The evidence used was LLM inference, which detected the modification of the object pointed to by `ptr`.