The rule applies here because the function `only_default` has a non-void return type, indicating that it is expected to return a value. The code violates this rule because the switch statement in `only_default` does not have a return statement for all cases, and the default case only increments the value without returning it, leaving the function's return value undefined. This is evident from the LLM inference, which detected that the function does not have an explicit return statement with an expression in all exit paths.