The rule applies here because it requires every switch statement to have at least one case clause, which includes non-empty case clauses. The code violates this rule because the switch statements in the functions `only_default` and `default_not_last` have a default case clause without any preceding non-empty case clause. This is evident from the LLM inference, which identifies the switch statements with only a default case clause as non-compliant.