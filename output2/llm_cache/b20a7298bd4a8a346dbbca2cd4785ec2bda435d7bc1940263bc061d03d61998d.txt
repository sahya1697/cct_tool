CORRECTED:
int logical_nonprimary(int a,int b,int c){if((a+b) && (c-1)) return 1; else return 0;}

EXPLANATION:
Parentheses were added to ensure that the operands of the logical && operator are primary-expressions.