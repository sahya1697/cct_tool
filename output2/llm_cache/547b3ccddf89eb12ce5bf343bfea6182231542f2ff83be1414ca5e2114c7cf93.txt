The rule applies here because the function prototype `m(double,double);` is a pointer parameter in a function prototype, even though it is not explicitly declared as a pointer. The code violates this rule because it does not declare the `double` parameters as `const`, even though they are not used to modify the addressed objects. The evidence used was LLM inference, which detected the function prototype as a pointer parameter in a function prototype.