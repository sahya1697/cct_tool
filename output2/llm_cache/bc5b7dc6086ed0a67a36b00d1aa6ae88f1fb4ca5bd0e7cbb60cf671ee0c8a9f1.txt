The MISRA C 2004 compliance rule 12.2 applies here because it prohibits relying on the order of evaluation of sub-expressions, which is unspecified in this code. The code violates this rule by using the expression `(n * 2)` in the `for` loop condition without any sequence points between the multiplication and the comparison, allowing the order of evaluation of `n` and `2` to vary. This is evidenced by the LLM inference that the expression is evaluated without any sequence points between the multiplication and the comparison.