CORRECTED:
    int logical_rhs(int const *p){if(p!=0 && *p++==42) return 1; return 0;}

EXPLANATION:
The pointer parameter in the function prototype was modified to be a pointer to const to reflect that it is not used to modify the addressed object.