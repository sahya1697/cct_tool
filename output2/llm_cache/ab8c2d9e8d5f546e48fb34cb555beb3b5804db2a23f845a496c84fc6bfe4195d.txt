The rule applies here because the function prototype and definition have pointer parameters, which are relevant to the rule. The code violates the rule because the function definition has pointer parameters that are not declared as pointer to const, despite not being used to modify the addressed object. The evidence used was LLM inference, which detected the pointer parameters in the function prototype and definition.