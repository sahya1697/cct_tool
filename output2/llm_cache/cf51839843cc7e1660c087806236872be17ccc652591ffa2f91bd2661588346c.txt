The rule applies here because the code snippet appears to be a function call, but the function name 'only_default' is not provided, implying that its prototype is being referenced. The code violates the rule because the function 'only_default' likely has a pointer parameter that is not used to modify the addressed object, but its prototype is not shown, making it unclear whether the pointer is declared as a pointer to const. The evidence used is the LLM inference, which suggests that the function prototype is being referenced, but its definition is not provided.