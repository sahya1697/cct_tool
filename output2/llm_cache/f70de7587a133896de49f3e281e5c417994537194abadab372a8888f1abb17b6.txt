The rule applies here because the code contains logical operators (&& and ||) that can lead to short-circuit evaluation, where the right-hand operand may not be evaluated if the left-hand operand is false. The code violates this rule because the right-hand operand of the logical && operator in the function logical_rhs contains a side effect, specifically the increment of the pointer p. This is evident from the LLM inference, which identified the logical && operator and the right-hand operand (*p++==42) as the source of the side effect.