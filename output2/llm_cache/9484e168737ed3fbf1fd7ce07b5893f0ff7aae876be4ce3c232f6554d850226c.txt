The rule applies here because the function `logical_rhs` has a pointer parameter `p` that is not used to modify the addressed object, but its type is not qualified with `const`. The code violates this rule because the function prototype for `logical_rhs` does not declare `p` as a pointer to `const`, despite not modifying the object it points to. This is inferred from the LLM (Language Model) analysis, which detected the function's prototype and identified the pointer parameter `p` as not being used for modification.