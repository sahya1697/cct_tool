The rule applies here because the function parameter 'a' is a pointer, even though it's not explicitly declared as one. The code violates this rule because the pointer parameter 'a' is not declared as a pointer to const, despite not being used to modify the addressed object. This is evident from the LLM inference, which correctly identifies the parameter 'a' as a pointer.