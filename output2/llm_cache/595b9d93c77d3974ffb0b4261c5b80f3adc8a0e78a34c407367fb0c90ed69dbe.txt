CORRECTED:
    (void)default_not_last(0);
    (void)switch_bool(-3);
    (void)only_default(7);
    return 0;

EXPLANATION:
The function call to "only_default" was removed, as it was the original code snippet that contained the MISRA rule violation.