CORRECTED:
int main(void){
    bad_scope(0); missing_break(0); default_not_last(0); switch_bool(0); only_default(7); return 0;
}

EXPLANATION:
The fix is to remove the constant expression -3 from the switch_bool function call, which is not allowed by MISRA C 2004 as it relies on the order of evaluation.