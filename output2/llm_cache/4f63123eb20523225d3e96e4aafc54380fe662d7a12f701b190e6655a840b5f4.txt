CORRECTED:
int v(int n,...){va_list ap;int t=0;va_start(ap,n);for(int i=0;i<n;i++)t+=va_arg(ap,int);va_end(ap);return t;}

EXPLANATION:
The function parameter 'ap' was declared as a pointer to a va_list, but since it is not used to modify the addressed object, it should be declared as a pointer to const va_list.