CORRECTED:
    int default_not_last(int v){int r=0;switch(v){default:r=9;break;case 0:r=1;break;}return r;}
    int switch_bool(int v){switch(v>0){case 0:return 0;case 1:return 1;default:return 2;}}
    int only_default(int v){int temp = v;switch(temp){default:temp++;break;}return temp;}
    
    int main(void){
    
EXPLANATION:
The fix is to introduce a new local variable to hold the value of `v` before it is used in the switch statement.