The rule applies here because the function h() has a parameter 'c' which is not explicitly declared as a pointer, but its usage in the expression 'c|d' implies it is being dereferenced. The code violates the rule because it does not declare 'c' as a pointer to const, despite not modifying the object it points to. The evidence used was LLM inference, which detected the implicit pointer usage in the expression.