CORRECTED:
int logical_rhs(int *p){if(p!=0 && *p==42) return 1; return 0;}
EXPLANATION:
Removed the post-increment operator to prevent side effects in the right-hand operand of the logical && operator.