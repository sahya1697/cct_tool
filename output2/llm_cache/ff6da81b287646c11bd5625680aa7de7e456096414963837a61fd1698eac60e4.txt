The rule applies here because the function `float_for` takes no parameters, but the rule's amplification text implies that it should be applied to all function prototypes, regardless of parameter type. The code violates the rule because it does not declare the function parameter `void` as a pointer to const, even though it is not used to modify the addressed object. The evidence used was LLM inference, which detected the function prototype as a potential candidate for the rule.