The rule applies here because the function 'assignb' is called with a pointer parameter, which is not explicitly declared as a pointer to const. The code violates this rule because the function 'assignb' is not shown to modify the object pointed to by its parameter, yet it does not declare the parameter as a pointer to const. The evidence used is the function call 'assignb(0,1)' and the function definition 'int assignb(int x,int y)'.