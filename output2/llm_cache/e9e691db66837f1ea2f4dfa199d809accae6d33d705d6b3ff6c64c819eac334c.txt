The rule applies here because the code contains function calls that pass integer arguments to functions, which may implicitly convert these integers to floating-point types. The code violates this rule because the function calls `bad_scope(0)`, `missing_break(0)`, `default_not_last(0)`, `switch_bool(-3)`, and `only_default(7)` pass integer arguments to functions, which may trigger implicit conversions. The evidence used is LLM inference, as the rule description mentions implicit conversions between integer and floating types, and the code's function calls match this pattern.