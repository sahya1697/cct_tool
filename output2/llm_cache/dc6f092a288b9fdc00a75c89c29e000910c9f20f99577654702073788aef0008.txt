The rule applies here because the function `for_side` has a pointer parameter, which is a potential candidate for the rule. The code violates this rule because the function `for_side` is called with no indication that the pointer parameter is used to modify the addressed object, suggesting that it should be declared as a pointer to const. The evidence used was LLM inference, which detected the presence of a pointer parameter in the function call.