CORRECTED:
    int size_effect(void){int i=0;return sizeof(int);}
    int logical_rhs(int *p){if(p!=0 && *p==42) return 1; return 0;}
    int logical_nonprimary(int a,int b,int c){if(a+b && c-1) return 1; else return 0;}
    
    int main(void){
    
EXPLANATION:
The sizeof operator is now applied to the type int, rather than an expression containing a side effect.