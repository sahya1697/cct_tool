The rule applies here because it requires explicit type declaration for all objects and functions, which includes pointers. The code violates this rule by declaring the pointer 'p' without explicitly stating its type, which is a pointer to a const int. This is evident from the LLM inference, which detected the implicit type declaration of the pointer 'p'. The correct declaration should be 'const int *p' to explicitly state its type.