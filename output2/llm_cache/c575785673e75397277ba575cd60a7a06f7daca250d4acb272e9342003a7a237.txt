The rule applies here because the function explicit_zero() returns a value indicating whether a condition is true or false, which could be interpreted as error information. The code violates this rule because it does not test the returned value, which could indicate an error, but instead relies on the function's behavior to be consistent with a boolean return. The evidence used was LLM inference, as the rule's applicability to this function was unclear and required review.