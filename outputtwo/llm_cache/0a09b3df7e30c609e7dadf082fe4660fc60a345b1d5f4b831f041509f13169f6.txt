The rule applies here because it requires explicit type declarations for all objects and functions, ensuring clarity and preventing implicit type assumptions. The code violates this rule because the parameter `len` is declared with an explicit type of `uint32_t`, but the `(void)len;` statement effectively ignores its type, making it unclear what type is actually being used. The evidence used is the LLM inference, which detected the implicit type usage in the function parameter.