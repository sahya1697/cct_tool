Rule 16.7 applies because the function `add` is declared with pointer parameters that are not qualified as `const`. The call `add(i++, i++)` passes two arguments that each modify `i` without a sequence point, so the function’s prototype violates the rule by exposing non‑const pointers that are never used to modify the addressed object. The AST shows a `CallExpr` with two `PostIncExpr` arguments, which matches the pattern of a function prototype that should use `const`‑qualified pointers, and the LLM inference confirms that the prototype is non‑const while the arguments are not modified through the pointers.