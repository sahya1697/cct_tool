CORRECTED:
int check_limits(int x, int y) {
        if ((x > 0) && (y < 100) && (x != y)) { return 1; }
        return 0; }

EXPLANATION:
Parentheses were added around each operand of the logical AND operator to ensure they are primary-expressions.